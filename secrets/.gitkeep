# Encrypted sops secrets live here as secrets.yaml (committed, encrypted).
# Create it with:  sops secrets/secrets.yaml
# It does not exist yet — modules/secrets.nix references it but declares no
# secrets, so the build works without it. Add it when you have a first secret.
